#include "uv/fs.ceu"

var&? UV_FS_Open o = spawn UV_FS_Open("/non/existent/path", _,_,_);
var int? err =
    watching o do
        await o.file.ok;
        // file is ready for use
        _printf("open ok\n");
    end;
if err? then
    _printf("open error: %d\n", err!);
end

escape 0;
