#include "uv/fs.ceu"

var&? UV_FS_Open f = spawn UV_FS_Open("/non/existent/path", _O_RDONLY, 0);
var int? err =
    watching f do
        await f!.file.ok;
        // file is ready for use
        _printf("open ok\n");
    end;
if err? then
    _printf("open error: %d\n", err!);
end

escape 0;
